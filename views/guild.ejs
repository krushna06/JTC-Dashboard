<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Guild</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <h1>Active Channels for Guild: <%= guildId %></h1>
        <ul>
            <% Object.keys(activeChannels).forEach(channelId => { %>
                <li>
                    Channel ID: <%= channelId %> <br>
                    Created At: <%= new Date(activeChannels[channelId].createdAt).toLocaleString() %> <br>
                    Owner ID: <%= activeChannels[channelId].ownerId %>
                </li>
            <% }) %>
        </ul>
        <a href="/dashboard" class="btn">Back to Dashboard</a>
    </div>
</body>
</html>
