<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Guild</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <h1>Active Channels for Guild: <%= guildId %></h1>
        <div class="channels-container">
            <% if (Object.keys(activeChannels).length > 0) { %>
                <% Object.keys(activeChannels).forEach(channelId => { %>
                    <div class="channel-box">
                        <h2>Channel ID: <%= channelId %></h2>
                        <p><strong>Created At:</strong> <%= new Date(activeChannels[channelId].createdAt).toLocaleString() %></p>
                        <p><strong>Owner ID:</strong> <%= activeChannels[channelId].ownerId %></p>
                    </div>
                <% }) %>
            <% } else { %>
                <p>No active channels found.</p>
            <% } %>
        </div>
        <a href="/dashboard" class="btn">Back to Dashboard</a>
    </div>
</body>
</html>
